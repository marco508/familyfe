// app/(app)/(tabs)/taches.tsx
// Les tâches (corvées) sont LA raison d'être de FamiLyfe : elles passent en
// onglet principal. L'implémentation reste dans `(app)/taches.tsx` (895 lignes),
// on l'expose simplement ici comme route d'onglet.
export { default } from '../taches';
