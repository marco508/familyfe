# FastAPI et serveur
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Base de données (SQLite — suffisant pour le dev local, pas de build natif)
sqlalchemy==2.0.36
databases[aiosqlite]==0.9.0
aiosqlite==0.20.0
# NB : les drivers PostgreSQL (asyncpg, psycopg2) sont dans requirements-postgres.txt
# — nécessaires uniquement en Docker/prod, pas pour un dev local en SQLite.

# Sécurité et authentification
python-jose[cryptography]==3.5.0
bcrypt==4.2.1
python-multipart==0.0.20

# Variables d'environnement
python-dotenv==1.0.1

# Validation de données
pydantic==2.10.3
pydantic-settings==2.6.1

# Utilitaires
typing-extensions>=4.7.0

# Rate limiting (en mémoire, pas de serveur externe requis)
slowapi==0.1.9

# Tâches planifiées en arrière-plan (rotations, gages, anniversaires)
apscheduler==3.11.0

# Migrations de base de données
alembic==1.14.0

# Client HTTP (notifications push Expo — best-effort)
httpx>=0.27.0
